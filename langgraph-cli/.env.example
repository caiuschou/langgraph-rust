# OpenAI API (.env loaded from project root or current directory)
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
# Optional: sampling temperature 0–2, lower is more deterministic (e.g. 0.2 to reduce tool misuse)
# OPENAI_TEMPERATURE=0.2
# Optional: tool choice auto|none|required
# OPENAI_TOOL_CHOICE=auto

# Exa MCP (web search). Enable with --mcp-exa when EXA_API_KEY is set.
# EXA_API_KEY=
# MCP_EXA_URL=https://mcp.exa.ai/mcp
# MCP_REMOTE_CMD=npx
# MCP_REMOTE_ARGS=-y mcp-remote
